# customized-skins-for-openmp
A reusable custom skin system for open.mp servers, built as a standalone filterscript. It allows server owners to register custom `.dff` and `.txd` character models, automatically distribute them through open.mp's artwork system, and assign custom skins to players without modifying their existing gamemode.
